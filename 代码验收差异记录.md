# 代码验收差异记录

> 2026-06-20：对照三个文档逐条核对实际代码。
>
> 另有独立文档 `前端修复规划.md` 记录了 25 个已知问题（5 Critical / 10 High / 10 Medium），全部未修复。

---

## 一、`规划.md` 逐项核查

### 工作流程（L5）
| 文档内容 | 实际 | 差异 |
|----------|------|------|
| `node --test tests/core.test.mjs` | package.json 中是 `tests/*.test.mjs`（运行全部测试文件） | 轻微：文档只写 core |

### 阶段一：预览与下载增强

| 步骤 | 文档 | 代码核查 | 结论 |
|------|------|----------|------|
| 1.1 后端 ZIP 流式生成工具 | ✅ 已完成 4 tests | `zip-stream.js` 正确实现；`zip-stream.test.mjs` 有 4 个测试 | ✅ 代码符合 |
| 1.2 批量/文件夹 ZIP 下载 API | ✅ 已完成 83 core tests | `zip-download.js` 存在但有**运行时崩溃 bug**（L29-34，`assertCompleteListing` 返回 void） | ❌ 有 Bug |
| 1.3 前端 ZIP 下载按钮 | ✅ 已完成 87 tests | `shared.js:89` 按钮、`events/index.js:707` 事件、`thunks.js:671` 逻辑 | ✅ 代码符合 |
| 1.4 视频/音频进度条 — Content-Length | ✅ 已完成 87 tests | `file-reads.js:178-181` 设置 Content-Length，支持 Range 请求 | ✅ 代码符合 |
| 1.5 前端文件夹右键下载 | ❌ 取消 | 代码中确实没有 | ✅ 文档准确 |

### 阶段二：拖拽上传

| 步骤 | 文档 | 代码核查 | 结论 |
|------|------|----------|------|
| 2.1 `app.dragging` + `setDragging` | ✅ 已完成 | `store.js:64` 初始状态，`:187-189` reducer，`events/index.js:1178,1190,1197` 调度 | ✅ 代码符合 |
| 2.2 `readDroppedEntries` 递归解析 | ✅ 已完成 | `helpers.js:21-46` 存在但 **`readEntries` 只调用一次**，>100 项目录静默丢文件 | ❌ 有 Bug |
| 2.3 dragenter/dragover/leave/drop 事件 | ✅ 已完成 | `events/index.js:1174-1203` 正确实现，dragCounter 防抖 | ✅ 代码符合 |
| 2.4 drop 覆盖层 + CSS | ✅ 已完成 | `index.js:283,309-319` 渲染，`components.css:2776-2810` 样式 | ✅ 代码符合 |

### 阶段三：分片上传断点续传

| 步骤 | 文档 | 代码核查 | 结论 |
|------|------|----------|------|
| 3.1 localStorage 工具函数 | ✅ 已完成 | `services/index.js:3-17`，含 getUploadKey/save/load/clearProgress | ✅ 代码符合 |
| 3.2 开始上传时保存 uploadId/parts | ✅ 已完成 | `services/index.js:115` | ✅ 代码符合 |
| 3.3 每分片完成后更新 localStorage | ✅ 已完成 | `services/index.js:138` | ✅ 代码符合 |
| 3.4 上传恢复时跳过已完成分片 | ✅ 已完成 | `services/index.js:88-99` 恢复检测，`:119-123` 跳过逻辑 | ✅ 代码符合 |
| **3.5 完成/取消时清理 localStorage** | ✅ 已完成 | 完成路径（L150）正确清理，但**取消路径不清理**：`onCancel=null`（`thunks.js:365`），workers 停止后试图 `complete` 失败，抛异常 → `clearProgress` 永不执行 | ❌ 有 Bug |

### 全局验收（L44-48）

| 项目 | 文档 | 实际 | 差异 |
|------|------|------|------|
| 全部测试通过 | ✅ 87/87 pass | 131/139 pass, **8 fail** | ❌ **严重不符** |
| 语法检查通过 | ✅ 69 files | 69 files OK | ✅ 符合 |
| 手动功能验证 | ⏳ | - | 合理（需浏览器） |

### 验收记录（L64-93）
所有步骤均为 ⏳ 未验收 — 符合文档状态。

---

## 二、`后续规划计划.txt` 逐项核查

### 一、当前真实状态（L7-26）

**L9-26 模块拆分列表**：17 个文件全部存在 ✅

**L36-49 已完成事项**：
- 入口拆分、shared.js/selectors/thunks/events/utils 拆分 — ✅ 全部确认
- L49 "`build` 与 `test` 当前通过" — ⚠️ **build 通过，test 8 个失败**

**L51-62 后台 11 个模块**：
- 模块 1-10（概览/健康/日志/配额/受保护路径/隐藏路径/存储空间/Webhook 配置/投递记录/分享管理）：✅ 全部代码完整
- **模块 11（通知历史面板）**：`renderAdminNotificationsSection` 函数已实现（`pages.js:890-930`），但 **`ADMIN_TABS` 中没有 `'notifications'` 标签**，函数从未被调用 → ❌ **未接入 UI**

**L64-74 Phase II-IV 功能**：全部确认 ✅

**L76-77 "当前仍然缺失的前端 UI"**：声称"无"，但通知历史标签页未接入 → ⚠️ 不准确

**L91-94 测试状态**：
- "后端 core tests: pass" → ✅ 通过
- "前端 frontend tests: 126 项通过" → ❌ **实际 131 项，但 8 项失败**

### 四、阶段规划（L144-189）

| 阶段 | 文档状态 | 实际 | 差异 |
|------|----------|------|------|
| A | ✅ 已完成 | index.js build 通过 | ✅ 符合 |
| B | ✅ 已完成 | ?mock=1 存在 | ✅ 符合 |
| C | ✅ 已完成 | utils/ 拆分完整 | ✅ 符合 |
| D | ✅ 已完成 | 渲染拆分完整 | ✅ 符合 |
| E | ✅ 已完成 | 后台模块完整 | ✅ 符合 |
| F | ✅ 已完成 | 功能验证通过 | ✅ 符合 |
| G | ✅ 已完成 | 但测试数从 35→139，非 115 | ⚠️ 测试数过时 |
| H | ✅ 已完成 | H-1 `hasPreview` 已删除 ✅；`requiresProtectedUnlock` 仍存在但简化 ✅；`original_key`→`fullKey` 优先 ✅。**`.topbar` 类在 CSS 中完全不存在**（H-3 声称 min-height 96→80px 无法验证）。"115/115 test pass" ❌ | ⚠️ 部分验证 |

### 五、Phase 验证（L191-297）

| Phase | 文档测试声称 | 实际 | 差异 |
|-------|-------------|------|------|
| I-5 | 115/115 test pass | 131/139, 8 fail | ❌ |
| IV 验证 | 117/117 test pass | 131/139, 8 fail | ❌ |
| V 验证 | 126/126 test pass | 131/139, 8 fail | ❌ |
| VI 验证 | 126/126 test pass | 131/139, 8 fail | ❌ |
| L297 | "当前 126 项测试全部通过" | 131/139, 8 fail | ❌ 严重不符 |

**II-3 "上传暂停/继续/取消/重试 UI ✅"**：UI 存在但**暂停无实际功能**（`thunks.js:443-449` 只弹 toast "暂不支持"）

**VI-3 "后台通知历史页面：新增「通知」tab"**：❌ `ADMIN_TABS` 中无 `'notifications'` 标签

**L271 "当前代码状态与这份规划完全一致，无冲突"**：❌ **多处冲突**

---

## 三、`项目功能.txt` 逐项核查

### 功能清单（L4-59）
36+ 项功能中 **35 项代码确认存在** ✅

| 行号 | 功能 | 结果 |
|------|------|------|
| L10 | 排序切换 | ⚠️ **列表视图排序按钮无事件处理**（`前端修复规划.md` M7） |
| L48 | 上传取消/重试 UI | ✅ 存在，但 **取消不清理 localStorage** |
| L52 | 上传暂停/继续 UI | ⚠️ **UI 存在但暂停无功能** |
| L56 | 后台通知历史管理页面「通知」tab | ❌ **tab 未接入 UI**（函数已写但 `ADMIN_TABS` 无此标签） |

其余 32 项全部确认存在 ✅

### L61-62 缺失项
声称"所有原计划功能均已补齐，暂无缺失项" — ⚠️ 排序切换无事件处理、通知 tab 未接入

### L64-65 当前明显问题
声称"暂无明显问题" — ❌ **与 `前端修复规划.md` 25 个已知问题严重矛盾**

### L67-68 结论
"测试 126/126 通过，无已知缺失项" — ❌ **131/139（8 fail），且有 25 个已知 issue**

---

## 四、运行验证结果（2026-06-20）

```
Unit tests:      131 pass / 8 fail  (139 total)
Syntax check:    69 files OK
Format check:    54 files (Prettier)  ❌
Build:           OK
```

---

## 五、所有三个文档共性问题汇总

| # | 问题 | 涉及文档 | 严重程度 |
|---|------|----------|----------|
| 1 | 测试通过率严重夸大（87/87、126/126 全绿 ← 实际 131/139） | 三个文档 | Critical |
| 2 | ZIP 下载文件夹崩溃（`assertCompleteListing` 返回值误用） | `规划.md` | Critical |
| 3 | 拖拽上传大目录（>100 项）静默丢文件 | `规划.md` | Critical |
| 4 | 取消分片上传不清理 localStorage + 服务端未 abort | `规划.md`、`项目功能.txt` | Critical |
| 5 | 通知历史面板未接入 admin tab 导航 | 三个文档 | High |
| 6 | 列表视图排序按钮无功能、全选无功能 | `项目功能.txt` | High |
| 7 | 上传暂停按钮无效（仅弹 toast） | 两个文档 | Medium |
| 8 | 格式检查 54 文件（Prettier） | — | Low |
| 9 | 测试数过时（文档声称 87/115/117/126，实际 139） | 三个文档 | Medium |
