{
  "name": "o-drive",
  "version": "2.1.0",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  },
  "scripts": {
    "build": "tailwindcss -i ./public/style.css -o ./public/main.css --minify",
    "test": "node --test tests/*.test.mjs"
  }
}
