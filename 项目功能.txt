**已补上（共 36+ 项）**
当前已实现的完整功能（前端 + 后端）：

- 登录 / 退出管理员
- 首页文件浏览
- 面包屑导航
- 搜索
- 网格 / 列表切换
- 前端类型筛选
- 排序切换
- 新建文件夹
- 普通文件上传
- 文件夹上传基础支持
- 文件预览
- 文本文件在线编辑并保存
- 文件下载
- 文件重命名
- 直链复制
- 创建分享链接
- 分享页访问
- 分享密码解锁
- 回收站浏览
- 回收站恢复
- 回收站彻底删除
- 回收站清空
- 批量选择
- 复制 / 移动剪贴板
- 粘贴到当前目录
- 批量删除到回收站
- 管理台概览页
- 管理端分享列表（含筛选、状态标签、直链复制）
- 删除分享
- 清理过期分享
- 操作日志（翻页 + 关键词搜索 + 动作类型筛选）
- 系统状态 / 健康检查（D1 / R2 / 环境变量状态检测）
- 存储配额管理（设置&强制校验）
- 访问密码路径管理（PBKDF2 加密 + 游客解锁流）
- 批量复制 / 移动的大任务托管（后端 Task 系统，含进度跟踪）
- 游客受保护路径解锁流（含延迟操作 + 频率限制）
- 隐藏路径管理（列表 / 添加 / 取消隐藏）
- 存储空间管理（R2 配额编辑 / S3 空间管理 / 路径绑定 / 连接测试）
- Webhook 配置（列表 / 添加 / 编辑 / 删除 / 测试投递）
- Webhook 投递记录（最近 20 条，状态 / 耗时 / 错误信息）
- 高级筛选面板（按类型 / 大小范围 / 日期范围）
- 搜索结果分页加载（cursor 分页 "加载更多"）
- 冲突处理策略选择（rename / overwrite / skip）
- 分片上传（文件 > 5MB 自动 multipart API）
- 上传取消 / 重试 UI
- Markdown 表格渲染
- Markdown 任务列表渲染
- 维护操作页面（快照 + 6 种操作 + 确认流程）
- 上传暂停 / 继续 UI
- 上传任务持久化（前端创建 / 更新 task 记录）
- 站内通知系统（D1 notifications 表 + 前端铃铛 + 下拉面板 + 30s 轮询）
- 通知桌面提醒（新通知推送浏览器 Notification API + Web Audio 双音效）
- 后台通知历史管理页面（「通知」tab / 表格展示 / 标记已读 / 刷选）
- 暗色模式（`[data-theme="dark"]` + `localStorage` + `prefers-color-scheme` 自动跟随 + light→dark→auto 三态循环）
- CSS 动画过渡（卡片抬升、modal scale-in、toast 滑入、prefers-reduced-motion）
- 深色模式跟随系统（toggle 循环 light → dark → auto，清除 localStorage 恢复自动跟随）

**缺失或未完全恢复**
所有原计划功能均已补齐，暂无缺失项。

**当前明显问题**
暂无明显问题。后台管理已扩展至 11 个模块（新增通知历史面板）。前端修饰器拆分已完成，持续可扩展。

**一句话结论**
所有规划功能已补齐，测试 126/126 通过，无已知缺失项。后续规划见 Phase VII–X（测试基建 → 功能扩展 → 体验优化 → 生态集成）。
