**已补上（共 36 项）**
当前已实现的完整功能（前端 + 后端）：

- 登录 / 退出管理员
- 首页文件浏览
- 面包屑导航
- 搜索
- 网格 / 列表切换
- 前端类型筛选
- 排序切换
- 新建文件夹
- 普通文件上传
- 文件夹上传基础支持
- 文件预览
- 文本文件在线编辑并保存
- 文件下载
- 文件重命名
- 直链复制
- 创建分享链接
- 分享页访问
- 分享密码解锁
- 回收站浏览
- 回收站恢复
- 回收站彻底删除
- 回收站清空
- 批量选择
- 复制 / 移动剪贴板
- 粘贴到当前目录
- 批量删除到回收站
- 管理台概览页
- 管理端分享列表（含筛选、状态标签、直链复制）
- 删除分享
- 清理过期分享
- 操作日志（翻页 + 关键词搜索 + 动作类型筛选）
- 系统状态 / 健康检查（D1 / R2 / 环境变量状态检测）
- 存储配额管理（设置&强制校验）
- 访问密码路径管理（PBKDF2 加密 + 游客解锁流）
- 批量复制 / 移动的大任务托管（后端 Task 系统，含进度跟踪）
- 游客受保护路径解锁流（含延迟操作 + 频率限制）
- 隐藏路径管理（列表 / 添加 / 取消隐藏）
- 存储空间管理（R2 配额编辑 / S3 空间管理 / 路径绑定 / 连接测试）
- Webhook 配置（列表 / 添加 / 编辑 / 删除 / 测试投递）
- Webhook 投递记录（最近 20 条，状态 / 耗时 / 错误信息）

**缺失或未完全恢复**
和原项目能力相比，还没恢复完整的有：

- 管理后台完整功能页 — 已有 10 个模块，还缺：维护操作 的 UI
- 大文件分片上传 — 后端完整（multipart create/part/complete/abort），前端未接入（仅用单 POST）
- 大文件分片上传 — 后端完整（multipart create/part/complete/abort），前端未接入（仅用单 POST）
- 上传暂停 / 继续 / 取消 / 重试 — 前端后端均无实现
- 上传任务进度管理 — 前端有队列，后端 Task 系统不执行 upload 类型任务
- 更完整的冲突处理策略 — 后端支持 4 种模式（error/overwrite/rename/skip），前端硬编码 rename
- 搜索的高级筛选 — 后端支持 kind/size/date 参数，前端仅发送文本查询
- 按大小筛选 / 按日期筛选 / 搜索结果继续加载 — 后端支持，前端无 UI
- 更完整的 Markdown / 文本预览体验 — 有自定义渲染器（129 行），不支持表格/任务列表等
- 移动端细节交互恢复 — 有响应式 CSS，无触屏专用交互优化

**当前明显问题**
已经接上的功能里，还有几处需要修：

- 回收站里的批量复制 / 移动 / 删除逻辑不合理，现在和普通文件区共用了同一套批量栏
- 文件夹上传目前是基础实现（逐目录创建），不等同于旧版完整上传器
- 搜索和筛选目前是精简版，前端未暴露高级筛选参数
- 管理台已有 10 个模块，尚缺：维护操作 UI

**一句话结论**
现在是"核心可用版"，不是"旧版全功能恢复版"。如果你要，我下一步可以直接按这个顺序继续补：`回收站批量逻辑`、`完整上传器`、`高级搜索筛选`。
